{
	"name": "falchion-result",
	"version": "0.1.1",
	"description": "A very minimal Result type for Typescript that is lightly inspired by Rust.",
	"main": "dist/index.js",
	"type": "module",
	"scripts": {
		"build": "tsc",
		"test": "vitest --run",
		"check": "[ dist/index.js -nt src/index.ts ] && exit 0 || npm run build",
		"dev": "esrun --watch dist/index.js",
		"start": "node dist/index.js"
	},
	"pre-commit": [
		"check"
	],
	"author": "Falchion Studios",
	"license": "GPL-3.0-only",
	"devDependencies": {
		"esrun": "^3.2.26",
		"pre-commit": "^1.2.2",
		"typescript": "^5.6.3"
	},
	"files": [
		"dist"
	],
	"types": "dist/index.d.ts"
}
